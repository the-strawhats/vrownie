import { Container } from '../../styles/global'
import SideBar from './components/SideBar'

const CartView = () => {
  return (
    <Container>
      <SideBar />
    </Container>
  )
}

export default CartView
